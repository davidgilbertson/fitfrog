{
  "name": "exercise-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.js",
    "test": "node --test",
    "check": "node scripts/check.js"
  },
  "engines": { "node": ">=22" }
}
